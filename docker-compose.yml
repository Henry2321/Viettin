version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=AIzaSyB6rodRS4YmnAU4-TAOH6SUidpP6Hmru-Y
    volumes:
      - ./backend:/app
    restart: unless-stopped